<view class="detail-page">
  <image class="detail-page__texture" src="/assets/analysis/group-40.svg" mode="scaleToFill" />
  <view class="detail-page__header">
    <navigation-bar title="内容分析" back="{{true}}" color="#000000" background="transparent" />
  </view>
  <view wx:if="{{detail}}" class="detail-page__content">
    <view class="detail-card">
      <view class="detail-card__inner">
        <view class="detail-card__head">
          <image class="detail-card__thumbnail" src="{{detail.card.thumbnailUrl}}" mode="aspectFill" />
          <view class="detail-card__identity">
            <text class="detail-card__title">{{detail.card.title}}</text>
            <text class="detail-card__date">{{detail.card.date}}</text>
          </view>
        </view>
        <view class="detail-card__metrics">
          <view wx:for="{{detail.card.metrics}}" wx:key="label" class="detail-card__metric">
            <text class="detail-card__metric-label">{{item.label}}</text>
            <text class="detail-card__metric-value">{{item.value}}</text>
          </view>
        </view>
      </view>
    </view>
    <view class="detail-intent">
      <text class="detail-intent__title">意向用户</text>
      <view class="detail-intent__panel">
        <view class="detail-intent__tabs" bindtouchstart="onIntentTouchStart" bindtouchend="onIntentTouchEnd">
          <view class="detail-intent__selection" style="transform: translateX({{intentTabOffset}}%);"></view>
          <view wx:for="{{intentTabs}}" wx:key="id" class="detail-intent__tab {{item.id === activeIntentLevel ? 'detail-intent__tab--active' : ''}}" bindtap="onIntentTabTap" data-index="{{index}}" data-id="{{item.id}}">{{item.label}}</view>
        </view>
        <view wx:if="{{hasVisibleIntentUsers}}" class="detail-users">
          <view wx:for="{{visibleIntentUsers}}" wx:key="id" class="detail-user" bindtap="onDetailUserTap" data-id="{{item.id}}">
            <image class="detail-user__avatar" src="{{item.avatarUrl}}" mode="aspectFill" />
            <view class="detail-user__body">
              <view class="detail-user__summary">
                <view class="detail-user__identity">
                  <text class="detail-user__name">{{item.name}}</text>
                  <text class="detail-user__tag detail-user__tag--{{item.level}}">#{{item.levelLabel}}</text>
                </view>
                <view class="detail-user__stats">
                  <view class="detail-user__stat"><text>阅读</text><text>{{item.readCount}}</text></view>
                  <view class="detail-user__divider"></view>
                  <view class="detail-user__stat"><text>完播</text><text>{{item.completionCount}}</text></view>
                  <view class="detail-user__divider"></view>
                  <view class="detail-user__stat"><text>转发</text><text>{{item.shareCount}}</text></view>
                </view>
              </view>
              <view class="detail-user__line"></view>
            </view>
          </view>
        </view>
        <view wx:else class="detail-intent__empty-state">
          <image class="detail-intent__empty-state__icon" src="/assets/analysis/empty-state-cloud.png" mode="aspectFill" />
          <text>没有意向用户</text>
        </view>
      </view>
    </view>
  </view>
</view>
